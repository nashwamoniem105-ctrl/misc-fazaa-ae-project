services:
  - type: web
    name: fazaa-landing
    buildCommand: "npm install && npm run build && npx tsc -p tsconfig.server.json"
    startCommand: "node dist-server/index.js"
    envVars:
      - key: ADMIN_PASSWORD
        value: Fazaa 2026
    healthCheckPath: /api/health
